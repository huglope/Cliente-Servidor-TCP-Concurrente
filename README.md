# Cliente-Servidor-TCP-Concurrente
En esta práctica se ha llevado acabo el desarrollo de un cliente y un servidor (concurrente) que permiten, mediante el protocolo TCP, la transformación de una cadena.
